<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Grup Oluştur</title>
  <link rel="stylesheet" href="/css/grupCreate.css" />
  <style>
    :root {
      /* Renk Paleti */
      --primary-color: #6200ea;
      --primary-light: #9d46ff;
      --primary-dark: #0a00b6;
      --secondary-color: #03dac6;
      --secondary-light: #66fff9;
      --secondary-dark: #00a896;
      --background-color: #f5f5f5;
      --navbar-color: #3700b3;
      --hero-gradient-start: #4facfe;
      --hero-gradient-end: #00f2fe;
      --cta-color: #ff6600;
      --cta-hover: #cc5200;
      --feature-bg: #ffffff;
      --feature-shadow: rgba(0, 0, 0, 0.1);
      --footer-bg: #333333;
      --footer-text: #ffffff;
      --link-color: #007bff;
      --link-hover: #0056b3;
      --font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      --border-radius: 10px;
      --transition-speed: 0.3s;
    }

    body {
      margin: 0;
      padding: 0;
      background-color: var(--background-color);
      font-family: var(--font-family);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    /* Navbar dahil edilirse, navbar için renk uyumu sağlamak amacıyla kendi stilinizi ekleyebilirsiniz */

    .container {
      display: flex;
      flex-direction: column;
      background-color: var(--feature-bg);
      border-radius: var(--border-radius);
      box-shadow: 0 4px 8px var(--feature-shadow);
      width: 800px; /* Genişliği artırdık */
      padding: 2em;
      text-align: center;
      gap: 1.5em;
    }

    h2 {
      color: var(--primary-dark);
      margin: 0;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1.5em;
    }

    input[type="text"] {
      border-radius: var(--border-radius);
      border: 1px solid #ccc;
      padding: 0.75em;
      font-size: 1em;
      transition: border-color var(--transition-speed);
    }

    input[type="text"]:focus {
      outline: none;
      border-color: var(--primary-light);
    }

    button {
      padding: 0.75em;
      background-color: var(--cta-color);
      color: #fff;
      border: none;
      border-radius: var(--border-radius);
      font-size: 1em;
      cursor: pointer;
      transition: background-color var(--transition-speed);
    }

    button:hover {
      background-color: var(--cta-hover);
    }
  </style>
</head>
<body>
  <%- include('./inc/navbar2') %>
  <div class="container">
    <h2>Grup Oluştur</h2>
    <form action="" method="post">
      <input type="text" id="name" name="name" required placeholder="Grup adı girin" />
    </label>
      <input type="text" id="memberUsernames" name="memberUsernames" placeholder="Kullanıcı adlarını girin (virgül ile ayırın)" />
      <button id="groupCreateButton">Oluştur</button>
    </form>
  </div>
  <script src="/script/groupCreate.js"></script>
</body>
</html>
